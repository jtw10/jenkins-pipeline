pipeline {
    agent none
    environment {
        PLACEHOLDER_VARIABLE = 'placeholder'
    }
    stages {
        stage("ECHO TEST") {
            steps {
                echo "test"
            }
        }
        stage("TEST BASH") {
            steps {
                sh "ls -l"
            }
        }
    }
}
